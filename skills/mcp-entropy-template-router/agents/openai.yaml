interface:
  display_name: "A2A MCP Bridge Router"
  short_description: "Routes embedding-aware A2A_MCP Node.js MCP bridge templates"
  default_prompt: "Use $mcp-entropy-template-router to route the A2A_MCP /mcp bridge as a Node.js or TypeScript template and generate `api_skill_tokens`, `style_temperature_profile`, and `template_route`."
